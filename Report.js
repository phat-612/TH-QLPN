class Report {
  constructor(reportId, reportedBy, reportedPlayerId, reason, timestamp) {
    this.reportId = reportId;
    this.reportedBy = reportedBy;
    this.reportedPlayerId = reportedPlayerId;
    this.reason = reason;
    this.timestamp = timestamp;
  }
}
